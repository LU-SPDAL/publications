# Primary simulations

`coverage_summary.csv` reproduces the aggregate values in Table 1. `robustness_summary.csv` collects the main 95% coverage averages from the additional simulation modules. `bandwidth_summary.csv` gives the bandwidth comparison reported in Section 4.3. `strong_persistence_summary.csv` records the N=216, phi=0.8 example discussed in the text.
