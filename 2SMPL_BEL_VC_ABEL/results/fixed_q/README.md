# Fixed block counts

`bridge_summary.csv` contains the compact bridge table used in the manuscript. The scripts in `code/fixed_q_reference.R`, `code/fixed_q_r1_summary.R` and `code/fixed_q_unequal_scale_check.R` generate the larger fixed-block-count reference calculations and the unequal-scale checks.
