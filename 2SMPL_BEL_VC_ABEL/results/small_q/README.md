# Small block counts

`bridge_table.csv` reproduces the compact small-block-count calibration table. The fixed-block-count reference calculations used for these comparisons are stored under `results/fixed_q`.
