# Brent application

`brent_table.csv` contains the values reported in Table 3. `application_summary.csv` records the sample dates, means, correction quantities and the ordinary-BEL domain used in the small-block-count illustration. The plotted profile is stored in `figures/figure2_brent_profile.pdf`.
